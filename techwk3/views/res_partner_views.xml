<odoo>
    <record id="res_partner_view_form" model="ir.ui.view">
        <field name="name">res.partner.view.form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <group name="sale" position="after">
                <group string="Available Products" name="product_list">
                    <field name="product_allowlist_ids" widget="one2many">
                        <tree string="Available Products" editable="bottom" domain="['|',('company_id','=',company_id),('company_id','=',false)]">
                            <field name="name"/>
                            <field name="list_price"/>
                        </tree>
                    </field>
                </group>
            </group>
        </field>
    </record>
</odoo>